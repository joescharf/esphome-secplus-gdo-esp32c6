# ESP32-C6 Garage Door Opener Configuration
# Copy this file to config.env and customize for your hardware

# =============================================================================
# GPIO Pin Configuration
# =============================================================================

# UART pins for Security+ communication
# These connect to the garage door opener's serial interface
GDO_TX_PIN=16        # GPIO for TX (to garage door opener)
GDO_RX_PIN=17        # GPIO for RX (from garage door opener)

# Optional: Status LED pin
# Comment out to disable status LED
STATUS_LED_PIN=23

# Optional: Hardware obstruction sensor pin
# If not set, protocol-based obstruction detection is used (recommended)
# OBSTRUCTION_PIN=18

# =============================================================================
# Device Configuration
# =============================================================================

# ESPHome device name (used in mDNS, Home Assistant entity IDs)
# Use lowercase letters, numbers, and hyphens only
DEVICE_NAME=garage-door

# Friendly name displayed in Home Assistant
FRIENDLY_NAME="Garage Door"

# =============================================================================
# Advanced Configuration (optional)
# =============================================================================

# ESP-IDF path for rebuilding gdolib from source
# If not set, the build script will attempt to auto-detect
# IDF_PATH=/path/to/esp-idf
